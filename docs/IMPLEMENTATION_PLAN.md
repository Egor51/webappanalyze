# План реализации MurmanClick Deal Engine

## Этап 1: Базовая структура (Текущий этап)

### ✅ Выполнено:
- [x] Создан компонент MandateForm (форма создания мандата)
- [x] Создана утилита mandateStorage для работы с мандатами
- [x] Базовая структура компонентов
- [x] Обновление Investing.jsx с новыми вкладками
- [x] Компонент MandatesList (список мандатов)
- [x] Компонент TradeIdeaCard (обновленные карточки сделок)
- [x] Компонент EventsFeed (лента событий рынка)
- [x] Компонент InvestorProfile (профиль инвестора)
- [x] Компоненты ExpertModal и CheckupModal (исполнение сделки)
- [x] Утилиты для расчетов потенциала и рисков
- [x] Интеграция сохранения сделок

---

## Этап 2: Слой 1 - Инвестиционный мандат (Buy-Box)

### Задачи:
1. **Компонент MandatesList**
   - Отображение списка мандатов
   - Редактирование/удаление мандатов
   - Показ количества найденных объектов
   - Ограничения для FREE/PRO

2. **Интеграция с поиском**
   - Фильтрация объектов по мандатам
   - Автоматический поиск при создании мандата
   - Обновление счетчиков совпадений

3. **Монетизация мандатов**
   - FREE: 1 мандат, базовые параметры
   - PRO: 3-5 мандатов, расширенные настройки

---

## Этап 3: Слой 2 - Trade Ideas (Сделки)

### Задачи:
1. **Обновление карточек объектов**
   - Потенциал (ниже рынка на X%, маржа флипа)
   - Ожидаемая доходность
   - Риски (слабый район, малая ликвидность)
   - Уровень интереса (звезды)

2. **Расчет потенциала**
   - На основе differencePercent
   - Расчет маржи для флипа
   - Расчет доходности для аренды
   - Оценка рисков

3. **Детальный просмотр**
   - FREE: базовая информация + уровень интереса
   - PRO: полная модель, сценарии, cash-flow

4. **Платный доступ к модели**
   - Кнопка "Открыть полную модель - 199 ₽"
   - Или "Оформить PRO"

---

## Этап 4: Слой 3 - События рынка (Event Layer)

### Задачи:
1. **Компонент EventsFeed**
   - Лента событий в стиле биржевого терминала
   - Фильтры по типам событий
   - Автообновление

2. **Типы событий:**
   - 🔻 Цена снижена/выросла
   - 🔥 Новый объект под мандат
   - 🧊 Объект висит долго (торг возможен)
   - 📈 Всплеск сделок в районе
   - 💡 Паттерн успешной сделки

3. **API для событий**
   - Endpoint для получения событий
   - WebSocket для real-time обновлений (опционально)

4. **Монетизация:**
   - FREE: часть событий, с задержкой
   - PRO: полный поток, без задержки, пуши в Telegram

---

## Этап 5: Слой 4 - Исполнение сделки

### Задачи:
1. **Кнопка "Хочу обсудить с экспертом"**
   - Модальное окно с формой
   - Отправка лида партнеру
   - Интеграция с партнерской системой

2. **Инвест-Checkup (платный отчет)**
   - Проверка объекта (документы, риски)
   - Проверка района
   - Финансовая модель
   - Интеграция с платежной системой

3. **Партнерская система**
   - Регистрация партнеров
   - Управление лидами
   - Статистика конверсий

---

## Этап 6: Слой 5 - Профиль инвестора

### Задачи:
1. **Компонент InvestorProfile**
   - Список мандатов
   - Статистика по сделкам
   - Сохраненные объекты
   - Статусы сделок

2. **Трекинг сделок**
   - Статусы: Идея → В переговорах → Покупка → Ремонт → Сдаётся/Продан
   - Ручное обновление статусов
   - История изменений

3. **Аналитика портфеля**
   - Распределение по районам
   - Рекомендации по ребалансировке
   - Экспорт отчетов (PRO)

4. **Монетизация:**
   - PRO: расширенная аналитика, экспорт, рекомендации

---

## Этап 7: Обновление навигации

### Задачи:
1. **Новые вкладки в Investing:**
   - Сделки (Trade Ideas) - по умолчанию
   - События (Events)
   - Мандаты (Mandates)
   - Профиль (Profile)

2. **Обновление UI**
   - Современный дизайн вкладок
   - Иконки для каждой вкладки
   - Адаптивность

---

## Этап 8: Монетизация и PRO

### Задачи:
1. **Система подписок**
   - Компонент SubscriptionModal
   - Интеграция с платежной системой
   - Управление статусом PRO

2. **Ограничения FREE/PRO**
   - Проверка статуса в компонентах
   - Блокировки функций
   - Призывы к действию

3. **Платные функции**
   - Разовые покупки (модели, отчеты)
   - Интеграция с платежной системой

---

## Технические детали

### Новые API endpoints (нужно реализовать на бэкенде):

1. `GET /ads/invest/mandates` - получить мандаты пользователя
2. `POST /ads/invest/mandates` - создать мандат
3. `PUT /ads/invest/mandates/:id` - обновить мандат
4. `DELETE /ads/invest/mandates/:id` - удалить мандат
5. `GET /ads/invest/trade-ideas?mandateId={id}` - получить сделки по мандату
6. `GET /ads/invest/events` - получить события рынка
7. `POST /ads/invest/leads` - отправить лид эксперту
8. `POST /ads/invest/checkup` - заказать проверку объекта
9. `GET /ads/invest/profile` - получить профиль инвестора
10. `PUT /ads/invest/profile/deal-status` - обновить статус сделки

### Структура данных:

**Мандат:**
```json
{
  "id": "string",
  "name": "string",
  "budgetMin": number,
  "budgetMax": number,
  "strategy": "rent" | "flip" | "parking",
  "cities": ["string"],
  "districts": ["string"],
  "propertyTypes": ["string"],
  "targetYield": number,
  "maxRisk": "low" | "medium" | "high",
  "excludeOldBuildings": boolean,
  "createdAt": number
}
```

**Trade Idea (расширенный объект):**
```json
{
  ...existingInvestmentOption,
  "potential": {
    "marketDifference": number,
    "flipMargin": number,
    "rentalYield": number,
    "interestLevel": number // 1-5 звезд
  },
  "risks": ["string"],
  "scenario": {
    "type": "rent" | "flip",
    "cashFlow": [...],
    "roi": number
  }
}
```

**Событие:**
```json
{
  "id": "string",
  "type": "price_drop" | "new_match" | "long_listing" | "volume_spike" | "pattern",
  "title": "string",
  "description": "string",
  "objectId": "string",
  "timestamp": number,
  "priority": "low" | "medium" | "high"
}
```

---

## Приоритеты реализации

1. **Высокий приоритет:**
   - Слой 1: Мандаты (Buy-Box)
   - Слой 2: Trade Ideas (обновленные карточки)
   - Обновление навигации

2. **Средний приоритет:**
   - Слой 3: События рынка
   - Слой 5: Профиль инвестора

3. **Низкий приоритет:**
   - Слой 4: Исполнение сделки
   - Монетизация и PRO

---

## Следующие шаги

1. Завершить обновление Investing.jsx с новыми вкладками
2. Создать компонент MandatesList
3. Обновить карточки объектов в Trade Ideas
4. Реализовать расчет потенциала и рисков
5. Создать компонент EventsFeed

